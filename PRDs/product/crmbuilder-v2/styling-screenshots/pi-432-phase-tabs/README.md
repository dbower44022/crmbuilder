# PI-432 — phase-tab navigation captures (REQ-526, DEC-953)

Offscreen captures of the v2 desktop at 1440×900 against the live store
(engagement ENG-002, 2026-08-30), taken with the same harness as the v0.6
runbook (`../CAPTURE-RUNBOOK.md`).

| File | Shows |
|---|---|
| `phase-1-business-context-capture.png` | Pinned Chat tab + Phase 1 tab; Every-session group; six numbered steps with ✓/▶ markers; collapsed All panels; Quick open in the header |
| `phase-13-verification.png` | Three open phase tabs in PRD order; Phase 13 steps; the DEC-721 Reconcile grid unchanged at its new address |
| `every-session-decisions.png` | Decisions opened from the Every-session group inside the Phase 13 tab |

These supersede the sidebar captures in `slice-A`…`slice-E` for the
navigation chrome; panel-level captures there remain valid.
