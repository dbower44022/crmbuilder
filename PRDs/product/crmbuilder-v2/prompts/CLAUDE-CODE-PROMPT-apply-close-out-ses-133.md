# Apply close-out — SES-133 / CNV-035

**Session:** SES-133 (Claude Code, planning). **Conversation:** CNV-035.
**Parent Project:** WS-014 (Governance & Delivery Redesign).
**Follows from:** SES-132 (the design session that produced the target model).

## What this close-out records

Resolves the seven remaining open decisions in §11 of
`governance-redesign-target-model.md` (#1–6 and #8; #7 and #9 were already
resolved as DEC-342/DEC-344). Five decisions:

- **DEC-345** — identifier-prefix scheme: Project → `PRJ-` (existing
  `WS-001..WS-014` migrated, not kept); delivery-phase Workstream → `WSK-`;
  Work Task → a **new** entity `WTK-` (not a refit of `work_ticket`).
- **DEC-346** — Planning Item lifecycle uses phase-agnostic state names
  (Draft → Decomposed → Ready → In Progress → In Review → Resolved,
  + Deferred/Cancelled).
- **DEC-347** — area priority is an ordinal layer rank on the System areas
  (storage 1 → access 2 → api 3 → mcp/ui 4; rest unranked).
- **DEC-348** — Engagement Areas are fully user-defined at engagement init,
  with **no** relationship to methodology Domains.
- **DEC-349** — the Workstream phase vocabulary adds `Design`.

Content deliverable: `governance-redesign-target-model.md` bumped to **v0.4**
(all nine §11 forks resolved; entity sections §2–§8 locked).

Addresses **PI-112** (settles its first work — the design decisions — without
resolving it; the migration itself is the next, separate session).

## Pre-flight

- API up on `127.0.0.1:8765`. Heads at author time: next DEC-345, SES-133,
  CNV-035 — no collision (single active session).
- Reference kinds validated against current vocab (matches SES-132's set,
  plus `session_follows_from` for session→session).

## Apply command

```bash
cd crmbuilder-v2
uv run python scripts/apply_close_out.py \
  ../PRDs/product/crmbuilder-v2/close-out-payloads/ses_133.json
```

## Notes from the actual apply

- One pre-flight validation reject on first run: session `executive_summary`
  was 804 chars (limit 200–800); trimmed to 797 and re-ran.
- The `addresses_planning_items` entry must be a dict
  `{"planning_item_identifier": "PI-112"}`, **not** a bare string `"PI-112"`
  (the bare-string form raises `TypeError` in `_shape_addresses_pi`). The core
  records had already POSTed by then; re-running is 409-safe (the script treats
  409 as already-present and skips), so only the addresses edge was newly
  created on the second run.
- Recorded as **deposit_event DEP-126**; log teed to
  `deposit-event-logs/dep_126.log`.

## Post-apply verification

- `decisions.json` count 330 → 335; DEC-345..349 present, all `Active`.
- `sessions.json` contains SES-133; `conversations.json` contains CNV-035.
- `references.json` contains `CNV-035 --addresses--> PI-112`.
- db-export snapshots regenerated by the apply; committed alongside the doc,
  payload, this prompt, and `dep_126.log`.
